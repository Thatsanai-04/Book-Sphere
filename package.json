{
  "name": "plot-bookstore-deployment",
  "private": true,
  "scripts": {
    "build": "npm --prefix client ci && npm --prefix client run build",
    "test": "npm --prefix server test"
  },
  "dependencies": {
    "@vercel/blob": "^2.0.0",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.9.3",
    "multer": "^2.3.0"
  },
  "devDependencies": {
    "@vercel/node": "latest"
  }
}
