import { useEffect, useState } from "react";
import { api } from "../api";

const emptyBook = { title: "", slug: "", synopsis: "", coverUrl: "", contentType: "novel", author: "", publisher: "", price: "", source: "url", fileUrl: "", contentHtml: "", isFree: false, status: "published" };

export default function AdminPage({ language, onLanguageChange, onBack, onCart }) {
  const english = language === "en";
  const [users, setUsers] = useState([]); const [books, setBooks] = useState([]);
  const [form, setForm] = useState(emptyBook); const [message, setMessage] = useState("");
  const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [uploading, setUploading] = useState(false);
  const set = (key, value) => setForm((current) => ({ ...current, [key]: value }));
  const load = async () => { setLoading(true); try { const [u, b] = await Promise.all([api("/admin/users"), api("/admin/books")]); setUsers(u.users); setBooks(b.books); } catch (error) { setMessage(error.message); } finally { setLoading(false); } };
  useEffect(() => { load(); }, []);
  useEffect(() => {
    const fileInput = document.querySelector('input[type="file"]');
    if (fileInput) fileInput.accept = ".epub,.pdf,.jpg,.jpeg,.png,.webp,application/epub+zip,application/pdf,image/jpeg,image/png,image/webp";
  }, []);
  const uploadFile = async (event) => {
    const file = event.target.files?.[0]; if (!file) return;
    setUploading(true); setMessage("");
    try { const data = new FormData(); data.append("file", file); const isCover = file.type.startsWith("image/"); const result = await api(isCover ? "/admin/uploads/cover" : "/admin/uploads/ebook", { method: "POST", body: data }); set(isCover ? "coverUrl" : "fileUrl", result[isCover ? "coverUrl" : "fileUrl"]); setMessage(`${isCover ? "Cover uploaded" : "Uploaded"}: ${result.fileName}`); }
    catch (error) { setMessage(error.message); } finally { setUploading(false); }
  };
  const uploadCover = async (event) => {
    const file = event.target.files?.[0]; if (!file) return;
    setUploading(true); setMessage("");
    try { const data = new FormData(); data.append("file", file); const result = await api("/admin/uploads/cover", { method: "POST", body: data }); set("coverUrl", result.coverUrl); setMessage(`Cover uploaded: ${result.fileName}`); }
    catch (error) { setMessage(error.message); } finally { setUploading(false); }
  };
  const createBook = async (event) => {
    event.preventDefault(); setSaving(true); setMessage("");
    try {
      const fileUrl = form.source === "writer" ? `inline://${form.slug}` : form.fileUrl;
      const { book } = await api("/admin/books", { method: "POST", body: JSON.stringify({
        title: form.title, slug: form.slug, synopsis: form.synopsis, coverUrl: form.coverUrl, contentType: form.contentType, format: "ebook", origin: "original",
        ebook: { fileUrl, contentHtml: form.source === "writer" ? form.contentHtml : undefined }, contributors: [{ name: form.author, role: "author" }],
        publisher: { name: form.publisher, type: form.publisher ? "publisher" : "independent" }, price: { amount: form.isFree ? 0 : Number(form.price), currency: "THB" }, isFree: form.isFree, status: form.status,
      }) });
      setBooks((items) => [book, ...items]); setForm(emptyBook); setMessage(english ? "E-Book added successfully" : "เพิ่ม E-Book เรียบร้อยแล้ว");
    } catch (error) { setMessage(error.message); } finally { setSaving(false); }
  };
  const updateUser = async (id, changes) => { try { const { user } = await api(`/admin/users/${id}`, { method: "PATCH", body: JSON.stringify(changes) }); setUsers((items) => items.map((item) => item._id === id ? { ...item, ...user } : item)); setMessage("User updated"); } catch (error) { setMessage(error.message); } };
  const updateBook = async (id, changes) => { try { const { book } = await api(`/admin/books/${id}`, { method: "PATCH", body: JSON.stringify(changes) }); setBooks((items) => items.map((item) => item._id === id ? { ...item, ...book } : item)); setMessage("Book updated"); } catch (error) { setMessage(error.message); } };
  const addBookToCart = async (slug) => {
    try {
      const cart = await api("/cart/items", { method: "POST", body: JSON.stringify({ slug }) });
      setMessage(`Added to cart (${cart.items.length} items)`);
    } catch (error) { setMessage(error.message); }
  };
  const addSampleBooks = async () => {
    try {
      const { books: samples } = await api("/admin/books/seed", { method: "POST" });
      setMessage(english ? `Created ${samples.length} sample E-Books` : `สร้าง E-Book ตัวอย่าง ${samples.length} รายการ`);
      await load();
    } catch (error) { setMessage(error.message); }
  };

  return <main className="min-h-screen bg-[#fffaf4]"><header className="border-b border-stone-200 bg-white"><div className="mx-auto flex h-[72px] max-w-6xl items-center justify-between px-5"><button onClick={onBack} className="text-lg font-black">← PLOT</button><div className="flex items-center gap-3"><div className="flex rounded-full border border-stone-200 bg-white p-1 text-xs font-bold" aria-label="เลือกภาษา"><button type="button" onClick={() => onLanguageChange("th")} className={`rounded-full px-2.5 py-1 ${!english ? "bg-ink text-white" : "text-stone-500"}`}>ไทย</button><button type="button" onClick={() => onLanguageChange("en")} className={`rounded-full px-2.5 py-1 ${english ? "bg-ink text-white" : "text-stone-500"}`}>EN</button></div><button onClick={onCart} className="rounded-full border border-stone-300 px-4 py-2 text-sm font-bold">{english ? "Cart" : "ตะกร้าสินค้า"}</button><span className="rounded-full bg-ink px-4 py-2 text-sm font-bold text-white">ADMIN CONSOLE</span></div></div></header><div className="mx-auto max-w-6xl px-5 py-10"><div className="flex flex-wrap items-start justify-between gap-4"><div><h1 className="text-3xl font-black">{english ? "Manage platform data" : "จัดการข้อมูลแพลตฟอร์ม"}</h1><p className="mt-2 text-stone-500">{english ? "Add E-Books from a URL, upload a file, or write directly in the browser." : "เพิ่ม E-Book จาก URL อัปโหลดไฟล์ หรือเขียนเนื้อหาโดยตรงในเบราว์เซอร์"}</p></div><button onClick={addSampleBooks} className="rounded-full bg-ink px-5 py-3 text-sm font-bold text-white">{english ? "Create sample E-Books" : "สร้าง E-Book ตัวอย่าง"}</button></div>{message && <p className="mt-5 rounded-xl bg-orange-50 px-4 py-3 text-sm text-orange-800">{message}</p>}
    <section className="mt-8 rounded-3xl bg-white p-6 shadow-sm ring-1 ring-stone-200"><h2 className="text-xl font-black">{english ? "Add E-Book" : "เพิ่ม E-Book"}</h2><form onSubmit={createBook} className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm font-bold">{english ? "Title" : "ชื่อเรื่อง"}<input required value={form.title} onChange={(e) => set("title", e.target.value)} className="mt-1 w-full rounded-xl border border-stone-200 p-3 font-normal" /></label><label className="text-sm font-bold">Slug<input required pattern="[a-z0-9-]+" value={form.slug} onChange={(e) => set("slug", e.target.value.toLowerCase())} className="mt-1 w-full rounded-xl border border-stone-200 p-3 font-normal" placeholder="my-book-title" /></label><label className="text-sm font-bold">{english ? "Author" : "ผู้เขียน"}<input required value={form.author} onChange={(e) => set("author", e.target.value)} className="mt-1 w-full rounded-xl border border-stone-200 p-3 font-normal" /></label><label className="text-sm font-bold">{english ? "Publisher" : "สำนักพิมพ์"} <span className="font-normal text-stone-400">({english ? "optional" : "ไม่บังคับ"})</span><input value={form.publisher} onChange={(e) => set("publisher", e.target.value)} className="mt-1 w-full rounded-xl border border-stone-200 p-3 font-normal" placeholder={english ? "Blank = independent" : "เว้นว่าง = นักเขียนอิสระ"} /></label><label className="text-sm font-bold">{english ? "Content type" : "ประเภทเนื้อหา"}<select value={form.contentType} onChange={(e) => set("contentType", e.target.value)} className="mt-1 w-full rounded-xl border border-stone-200 p-3 font-normal"><option value="novel">{english ? "Novel" : "นิยาย"}</option><option value="comic">{english ? "Comic" : "การ์ตูน"}</option><option value="magazine">{english ? "Magazine" : "นิตยสาร"}</option><option value="newspaper">{english ? "Newspaper" : "หนังสือพิมพ์"}</option></select></label><label className="text-sm font-bold">{english ? "Price (THB)" : "ราคา (บาท)"}<input required={!form.isFree} disabled={form.isFree} min="0" type="number" value={form.isFree ? 0 : form.price} onChange={(e) => set("price", e.target.value)} className="mt-1 w-full rounded-xl border border-stone-200 p-3 font-normal disabled:bg-stone-100" /></label><label className="text-sm font-bold sm:col-span-2">{english ? "Synopsis" : "เรื่องย่อ"}<textarea required value={form.synopsis} onChange={(e) => set("synopsis", e.target.value)} className="mt-1 min-h-24 w-full rounded-xl border border-stone-200 p-3 font-normal" /></label><fieldset className="rounded-xl border border-stone-200 p-4 sm:col-span-2"><legend className="px-1 text-sm font-bold">{english ? "E-Book source" : "แหล่งที่มาของ E-Book"}</legend><div className="flex flex-wrap gap-4 text-sm font-medium"><label><input type="radio" checked={form.source === "url"} onChange={() => set("source", "url")} /> {english ? "File URL" : "URL ไฟล์"}</label><label><input type="radio" checked={form.source === "upload"} onChange={() => set("source", "upload")} /> {english ? "Upload EPUB/PDF" : "อัปโหลด EPUB/PDF"}</label><label><input type="radio" checked={form.source === "writer"} onChange={() => set("source", "writer")} /> {english ? "Write in browser" : "เขียนในเบราว์เซอร์"}</label></div>{form.source === "url" && <input required type="url" value={form.fileUrl} onChange={(e) => set("fileUrl", e.target.value)} className="mt-4 w-full rounded-xl border border-stone-200 p-3" placeholder="https://storage.example/book.epub" />}{form.source === "upload" && <div className="mt-4"><input required type="file" accept=".epub,.pdf,application/epub+zip,application/pdf" onChange={uploadFile} /><p className="mt-2 text-xs text-stone-500">{english ? "EPUB or PDF, maximum 50 MB." : "EPUB หรือ PDF ขนาดไม่เกิน 50 MB"} {form.fileUrl && (english ? "File ready to save." : "ไฟล์พร้อมบันทึกแล้ว")}</p></div>}{form.source === "writer" && <textarea required value={form.contentHtml} onChange={(e) => set("contentHtml", e.target.value)} className="mt-4 min-h-56 w-full rounded-xl border border-stone-200 p-3" placeholder={english ? "Write your E-Book content here. Basic HTML is supported." : "เขียนเนื้อหา E-Book ที่นี่ รองรับ HTML พื้นฐาน"} />}</fieldset><label className="flex items-center gap-2 text-sm font-bold"><input type="checkbox" checked={form.isFree} onChange={(e) => set("isFree", e.target.checked)} /> {english ? "Free download" : "ดาวน์โหลดฟรี"}</label><label className="text-sm font-bold">{english ? "Status" : "สถานะ"}<select value={form.status} onChange={(e) => set("status", e.target.value)} className="ml-3 rounded-xl border border-stone-200 p-2 font-normal"><option value="draft">{english ? "Draft" : "ฉบับร่าง"}</option><option value="published">{english ? "Published" : "เผยแพร่"}</option></select></label><button disabled={saving || uploading || (form.source === "upload" && !form.fileUrl)} className="rounded-full bg-orange px-6 py-3 font-bold text-white disabled:opacity-60 sm:col-span-2">{saving ? (english ? "Adding…" : "กำลังเพิ่ม…") : uploading ? (english ? "Uploading…" : "กำลังอัปโหลด…") : (english ? "Add E-Book" : "เพิ่ม E-Book")}</button></form></section>
    {loading ? <p className="mt-8 text-stone-500">Loading…</p> : <div className="mt-10 space-y-10"><section><div className="mb-3 flex items-center justify-between"><h2 className="text-xl font-black">Users</h2><button onClick={load} className="text-sm font-bold text-orange-600">Refresh</button></div><div className="overflow-x-auto rounded-2xl bg-white ring-1 ring-stone-200"><table className="w-full min-w-[680px] text-left text-sm"><thead className="border-b border-stone-200 text-stone-500"><tr><th className="p-4">Name</th><th>Email</th><th>Roles</th><th>Status</th></tr></thead><tbody>{users.map((user) => <tr key={user._id} className="border-b border-stone-100 last:border-0"><td className="p-4 font-semibold">{user.displayName}</td><td>{user.email}</td><td><input defaultValue={user.roles.join(", ")} onBlur={(event) => { const roles = event.target.value.split(",").map((role) => role.trim()).filter(Boolean); if (roles.join(", ") !== user.roles.join(", ")) updateUser(user._id, { roles }); }} className="w-28 rounded border border-stone-200 px-2 py-1" /></td><td><select value={user.status} onChange={(event) => updateUser(user._id, { status: event.target.value })} className="rounded border border-stone-200 px-2 py-1"><option value="active">active</option><option value="suspended">suspended</option></select></td></tr>)}</tbody></table></div></section><section><h2 className="mb-3 text-xl font-black">Books</h2><div className="overflow-x-auto rounded-2xl bg-white ring-1 ring-stone-200"><table className="w-full min-w-[760px] text-left text-sm"><thead className="border-b border-stone-200 text-stone-500"><tr><th className="p-4">Title</th><th>Type</th><th>Seller</th><th>Price</th><th>Publishing status</th><th>Cart</th></tr></thead><tbody>{books.map((book) => <tr key={book._id} className="border-b border-stone-100 last:border-0"><td className="p-4 font-semibold">{book.title}</td><td>{book.contentType}</td><td>{book.seller?.displayName || "—"}</td><td>{book.isFree ? "Free" : `฿${book.price?.amount ?? 0}`}</td><td><select value={book.status} onChange={(event) => updateBook(book._id, { status: event.target.value })} className="rounded border border-stone-200 px-2 py-1"><option value="draft">draft</option><option value="pending_review">pending review</option><option value="published">published</option><option value="unpublished">unpublished</option></select></td><td>{book.status === "published" ? <button onClick={() => addBookToCart(book.slug)} className="rounded-full bg-orange px-3 py-2 text-xs font-bold text-white">เพิ่มลงตะกร้า</button> : <span className="text-xs text-stone-400">Publish ก่อน</span>}</td></tr>)}</tbody></table></div></section></div>}</div></main>;
}
